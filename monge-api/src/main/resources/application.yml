server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: monge-api
  
logging:
  level:
    br.ufpr.monge: INFO
    org.springframework.web: DEBUG
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

